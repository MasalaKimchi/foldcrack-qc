"""Fold/crack artifact quality-control feasibility toolkit.

The default package is intentionally dependency-light.  Foundation-model and
OME-TIFF capabilities are optional so that the engineering smoke test runs on
a CPU-only Mac before any large model or private WSI is introduced.
"""

__version__ = "0.1.0"
